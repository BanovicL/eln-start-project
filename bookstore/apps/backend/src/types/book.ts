export type Book = {
    id: string
    title: string
    genre: string
    pages: number
    description: string
    author: string
}

export type BookInput = {
    id: string
    title: string
    genre: string
    pages: number
    description: string
    author: string
}